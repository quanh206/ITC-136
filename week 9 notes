git config --global user.name/email sets name or email

if (x = 1); then 
  .....
elif (x = 2); then
  ....
else ....
fi

for variable in $(file); 
 do
done 

to clone repository cd ~/Desktop
git clone https://github.com/quanh206/ITC-136.git

to copy files from your vm to repository
cp /usr/local/bin/* . 

commit your changes
git add *
git commit -m "comment about the changes"
git push

vim example.sh
chmod +x example.sh
./example.sh to run the file
